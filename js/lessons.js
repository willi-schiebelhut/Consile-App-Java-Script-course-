// function checkAge(age) {
//     if (age > 18) {
//       return true;
//     } else {
//       return confirm('Родители разрешили?');
//     }
//   }

function min (a,b) {
    if (a < b) {
        return (a);
    } else {
        return (b);
    }
}

alert(min(9,1));